export const emailRegex = "^[a-z0-9!#$%&'*+/=?^_`{|}~-]+(?:\\.[a-z0-9!#$%&'*+/=?^_`{|}~-]+)*@(?:[a-z0-9](?:[a-z0-9-]*[a-z0-9])?\\.)+[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$";

export const shortMovieLength = 40;

export const LARGE_WINDOW_SIZE = 1280;
export const MEDIUM_WINDOW_SIZE = 999;
export const SMALL_WINDOW_SIZE = 575;

export const cardsInRowQuantity = {
    lg: 4,
    md: 3,
    s: 2,
    xs: 1,
}

export const defaultRowsOfCardsQuantity = {
    lg: 3,
    s: 4,
    xs: 5,
}